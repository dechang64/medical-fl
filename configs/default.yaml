model:
  backbone: tiny
  img_size: 64
  num_classes: 6

data:
  num_samples_per_client: 200
  noise_ratio: 0.2
  non_iid_alpha: 0.5

fl:
  num_clients: 5
  rounds: 50
  local_epochs: 3
  learning_rate: 0.001
  batch_size: 32
  aggregation: prototype_aware
